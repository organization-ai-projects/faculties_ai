[
  {
    "principle": "Single Responsibility Principle (SRP)",
    "description": "A class should have only one reason to change, meaning it should have only one responsibility.",
    "example": {
      "bad": "class UserManager {\n  add_user(user) { // Add user to database }\n  send_email(user) { // Send email }\n}",
      "good": "class UserManager {\n  add_user(user) { // Add user to database }\n}\nclass EmailService {\n  send_email(user) { // Send email }\n}"
    }
  },
  {
    "principle": "Open/Closed Principle (OCP)",
    "description": "Software entities should be open for extension but closed for modification.",
    "example": {
      "bad": "class Shape {\n  area() { }\n}\nclass Rectangle extends Shape {\n  area() { return width * height; }\n}",
      "good": "class Shape {\n  area() { }\n}\nclass Rectangle extends Shape {\n  area() { return width * height; }\n}\nclass Circle extends Shape {\n  area() { return Math.PI * radius * radius; }\n}"
    }
  },
  {
    "principle": "Liskov Substitution Principle (LSP)",
    "description": "Objects of a base class should be replaceable with objects of a derived class without affecting the functionality.",
    "example": {
      "bad": "class Rectangle {\n  set_width(width) { this.width = width; }\n  set_height(height) { this.height = height; }\n}\nclass Square extends Rectangle {\n  set_width(width) { this.width = this.height = width; }\n  set_height(height) { this.height = this.width = height; }\n}",
      "good": "class Shape {\n  area() { }\n}\nclass Rectangle extends Shape {\n  area() { return this.width * this.height; }\n}\nclass Square extends Rectangle {\n  area() { return this.side * this.side; }\n}"
    }
  },
  {
    "principle": "Interface Segregation Principle (ISP)",
    "description": "It is better to have many small, specific interfaces than one large, general-purpose interface.",
    "example": {
      "bad": "class Machine {\n  print() { }\n  scan() { }\n  fax() { }\n}",
      "good": "class Printer {\n  print() { }\n}\nclass Scanner {\n  scan() { }\n}\nclass Fax {\n  fax() { }\n}"
    }
  },
  {
    "principle": "Dependency Inversion Principle (DIP)",
    "description": "High-level modules should not depend on low-level modules. Both should depend on abstractions.",
    "example": {
      "bad": "class LightBulb {\n  turn_on() { }\n  turn_off() { }\n}\nclass Switch {\n  constructor(bulb) { this.bulb = bulb; }\n  operate() { this.bulb.turn_on(); }\n}",
      "good": "class Switchable {\n  turn_on() { }\n  turn_off() { }\n}\nclass LightBulb extends Switchable {\n  turn_on() { }\n  turn_off() { }\n}\nclass Switch {\n  constructor(device) { this.device = device; }\n  operate() { this.device.turn_on(); }\n}"
    }
  }
]
